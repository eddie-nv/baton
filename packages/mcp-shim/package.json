{
  "name": "@baton/mcp-shim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "baton-mcp": "./dist/bin.js"
  },
  "main": "./dist/server.js",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b && chmod +x dist/bin.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@baton/shared": "*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.0"
  }
}
